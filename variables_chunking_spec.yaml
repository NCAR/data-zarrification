# zarr requirement: chunk size divides evenly into dim size
ocn:
  pop.h:
    variable_category:
      2D:
        variables:
          - SST
          - SSH
        chunks:
          time: 12
          member_id: 40
          # nlat: 192
          # nlon: 160

      3D:
        variables:
          - SALT
        chunks:
          time: 12
          z_t: 1
          member_id: 40 
          # nlat: 192
          # nlon: 160

    frequency: monthly

ice:
  cice.h:
    variable_category:
      2D:
        variables:
          - aice_nh
          - aice_sh
          - hi_nh
          - hi_sh
        chunks:
          time: 12
    frequency: monthly

atm:
  cam.h0:
    variable_category:
      2D:
        variables:
          - T
          - U
          - v
          - Q
          - Z3
        chunks:
          time: 12
          # lat: 96
          # lon: 72
      3D:
        variables:
          - FLNS
          - FLNSC
          # - FLUT
          # - FSNS
          # - FSNSC
          # - FSNTOA
          # - ICEFRAC
          # - LHFLX
          # - PRECC
          # - PRECL
          # - PRECSC
          # - PRECSL
          # - PSL
          # - SHFLX
          # - TMQ
          # - TREFHT
          # - TREFHTMN
          # - TREFHTMX
          # - TS
        chunks:
          time: 12
    frequency: monthly
lnd:
  clm2.h0:
    variable_category:
      3D_2D:
        variables:
          - FSNO
          - H2OSNO
          - QRUNOFF
          - RAIN
          - SNOW
          - SOILWATER_10CM
          - SOILLIQ
        chunks:
          time: 12
          # lat: 96
          # lon: 144
          levgrnd: 1
          levlak: 1
     

    frequency: monthly
  # clm2.h1:
  #   variable_category:
  #     2D:
  #       variables:
  #         - FSNO
  #         - H2OSNO
  #         - QRUNOFF
  #         - RAIN
  #         - SNOW
  #         - SOILLIQ
  #         - SOILWATER_10CM
  #       chunks:
  #         time: 480
  #         lat: 96
  #         lon: 144
  #   frequency: daily
